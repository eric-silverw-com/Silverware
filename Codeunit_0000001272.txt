OBJECT Codeunit 1272 Exp. Validation Gen. Jnl.
{
  OBJECT-PROPERTIES
  {
    Date=10/25/16;
    Time=12:00:00 PM;
    Version List=NAVW110.00;
  }
  PROPERTIES
  {
    TableNo=81;
    OnRun=VAR
            GenJnlLine@1000 : Record 81;
          BEGIN
            DeletePaymentFileBatchErrors;
            DeletePaymentFileErrors;

            GenJnlLine.COPYFILTERS(Rec);
            IF GenJnlLine.FINDSET THEN
              REPEAT
                CODEUNIT.RUN(CODEUNIT::"Payment Export Gen. Jnl Check",GenJnlLine);
              UNTIL GenJnlLine.NEXT = 0;

            IF GenJnlLine.HasPaymentFileErrorsInBatch THEN BEGIN
              COMMIT;
              ERROR(HasErrorsErr);
            END;
          END;

  }
  CODE
  {
    VAR
      HasErrorsErr@1000 : TextConst 'ENU=The file export has one or more errors.\\For each line to be exported, resolve the errors displayed to the right and then try to export again.;ESM=La exportaci¢n de archivo tiene uno o m s errores.\\Para cada l¡nea que se debe exportar, resuelva todos los errores que se muestren a la derecha y despu‚s intente la exportaci¢n de nuevo.;FRC=L''exportation de fichier comporte une ou plusieurs erreurs.\\Pour chaque ligne … exporter, corrigez les erreurs affich‚es … droite, puis r‚essayez d''effectuer l''exportation.;ENC=The file export has one or more errors.\\For each line to be exported, resolve the errors displayed to the right and then try to export again.';

    BEGIN
    END.
  }
}

